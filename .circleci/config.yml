version: 2.1

orbs:
  node: circleci/node@7.1.0

jobs:
  test:
    executor: node/default
    steps:
      - checkout
      - node/install-packages:
          pkg-manager: npm
      - run:
        name: Run tests
        command: npm run test
      - persist_to_workspace:
        root: ~/project
        paths:
          - .

workflows:
  my-wordlist-mobile-workflow:
    jobs:
      - test
