import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  centered: {
    alignItems: 'center',
    flex: 1,
    justifyContent: 'center'
  },
  container: {
    flex: 1,
    justifyContent: 'center',
    margin: 'auto'
  }
});
