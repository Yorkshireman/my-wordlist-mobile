import { customColours } from './utils';
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: customColours.background,
    flex: 1,
    justifyContent: 'center',
    padding: 10
  }
});
